{
  "name": "jsonserver",
  "version": "1.0.0",
  "description": "Fake REST API for testing",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "json:server":"json-server --watch db.json", 
    "json:server:remote":"json-server http://jsonplaceholder.typicode.com/db",
    "json:servert":"json-server --watch tic-tac-toe-db.json",
    "json:server-todo":"json-server --watch to-do-list-db.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
